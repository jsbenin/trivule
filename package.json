{
  "name": "trivule-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter trivule build",
    "dev": "pnpm --filter trivule dev",
    "test": "pnpm --filter trivule test",
    "test:watch": "pnpm --filter trivule test-watch",
    "lint": "pnpm --filter trivule lint",
    "format": "pnpm --filter trivule format",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  }
}