# Trivule VS Code Extension

Auto-completion support for the Trivule form validation library.

## Features

- Auto-completion for Trivule attributes (`@v:rules`, `@v:msg`, etc.)
- Auto-completion for validation rules inside `@v:rules`
